export type WeatherData = {
    city: string;
    temperature: number;
    windSpeed: number;
    date: string;
    createdAt: string;
}
export type WeatherRecord = {
    city: string;
    date: string;
    temp: number;
    wind: number;
    savedAt: string;
}